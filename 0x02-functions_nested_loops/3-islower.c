#include "main.h"

/**
* _islower -> A function that checks for lowercase character
* @c: a charcter argument
* Return: Returns 1 if parameter is lowercase and 0 if otherwise
*/
int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
