#include "main.h"

/**
* mul -> A function that multiplies two integer values
* @a: First argument or parameter as int
* @b: Second argument or parameter as int
* Return: Success if 0
*/

int mul(int a, int b)
{
	return (a * b);
}
